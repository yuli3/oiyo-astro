// The foundation audit owns the contract checks. This named entry point makes the
// life-signal gate independently runnable without duplicating validation logic.
import "./audit-ontology-expansion-v2-foundation.mjs";
